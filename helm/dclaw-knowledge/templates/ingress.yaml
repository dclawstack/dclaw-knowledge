apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-knowledge.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-knowledge.name" . }}
spec:
  rules:
    - host: dclaw-knowledge.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-knowledge.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
